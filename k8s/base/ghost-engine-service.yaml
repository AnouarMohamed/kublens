apiVersion: v1
kind: Service
metadata:
  name: k8s-ops-ghost-engine
  namespace: kubernetes-operations-dashboard
  labels:
    app.kubernetes.io/name: k8s-ops-ghost-engine
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: k8s-ops-ghost-engine
  ports:
    - name: grpc
      port: 8091
      targetPort: grpc
